import { useEffect, useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { motion, AnimatePresence } from 'framer-motion';
import { ArrowRight, Loader2, Search, Check, X, Building2 } from 'lucide-react';
import { Input } from '@/components/ui/input'; import { Button } from '@/components/ui/button'; import { supabase } from '@/integrations/supabase/client'; import { applyBrand, resetBrand } from '@/lib/branding'; import { getCompanyLogoUrl } from '@/lib/companyLogo';
interface FoundCompany { id:string; name:string; slug:string; logo_url:string|null; primary_color:string; secondary_color:string; is_system:boolean; enabled_modules?:string[]; is_active?:boolean; }
const PUBLIC_COMPANY_CACHE='nuvexa:public-company:'; const COMPANY_CACHE='nuvexa:company:';
const readPublicCached=(query:string):FoundCompany|null=>{try{const raw=localStorage.getItem(`${PUBLIC_COMPANY_CACHE}${query.toLowerCase()}`);if(!raw)return null;const c=JSON.parse(raw) as FoundCompany;return c?.id&&c?.primary_color&&c?.secondary_color?c:null;}catch{return null;}};
const cachePublic=(query:string,c:FoundCompany)=>{try{localStorage.setItem(`${PUBLIC_COMPANY_CACHE}${query.toLowerCase()}`,JSON.stringify(c));localStorage.setItem(`${COMPANY_CACHE}${c.id}`,JSON.stringify(c));}catch{}};
export default function SelectCompany(){const navigate=useNavigate();const[query,setQuery]=useState('');const[debounced,setDebounced]=useState('');const[found,setFound]=useState<FoundCompany|null>(null);const[notFound,setNotFound]=useState(false);const[searching,setSearching]=useState(false);const[continuing,setContinuing]=useState(false);
 useEffect(()=>{const t=setTimeout(()=>setDebounced(query.trim()),120);return()=>clearTimeout(t)},[query]);
 useEffect(()=>{if(!debounced){setFound(null);setNotFound(false);resetBrand();return;}const cached=readPublicCached(debounced);if(cached){setFound(cached);setNotFound(false);applyBrand(cached);}let cancelled=false;setSearching(!cached);(async()=>{const{data,error}=await(supabase as any).rpc('public_find_company',{_query:debounced});if(cancelled)return;const row=Array.isArray(data)?data[0]:data;if(!error&&row){const company=row as FoundCompany;cachePublic(debounced,company);setFound(company);setNotFound(false);applyBrand(company);}else if(!cached){setFound(null);setNotFound(true);resetBrand();}setSearching(false)})();return()=>{cancelled=true}},[debounced]);
 const glow=useMemo(()=>!found?'transparent':`radial-gradient(closest-side, ${found.primary_color}, ${found.secondary_color} 40%, transparent 78%)`,[found]);
 const handleContinue=()=>{if(!found)return;setContinuing(true);setTimeout(()=>navigate(`/auth?company=${encodeURIComponent(found.slug)}`),100)};
 return <div className="min-h-screen flex flex-col items-center justify-center px-5 py-10 relative overflow-hidden transition-colors duration-300" style={{background:'radial-gradient(circle at 12% 0%, hsla(220,60%,55%,0.10), transparent 45%), radial-gradient(circle at 88% 100%, hsla(220,60%,45%,0.08), transparent 40%), #F5F7FB'}}>{found?.logo_url&&<div className="relative mb-8 flex items-center justify-center"><motion.div initial={false} animate={{opacity:1,scale:1}} transition={{duration:.3}} className="absolute inset-0 -m-20 rounded-full blur-3xl pointer-events-none" style={{background:glow}}/><motion.img key={found.id} src={getCompanyLogoUrl(found.logo_url)??undefined} alt={found.name} initial={{opacity:0,scale:.95}} animate={{opacity:1,scale:1}} transition={{duration:.25}} className="h-24 md:h-28 object-contain relative z-10 drop-shadow-[0_10px_30px_rgba(15,23,42,0.15)]" loading="eager"/></div>}<motion.div initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} transition={{duration:.3}} className="w-full max-w-md relative z-10"><div className="text-center mb-6"><h1 className="text-2xl md:text-[28px] font-bold text-[#12151C] tracking-tight" style={{fontFamily:'Sora, Inter, sans-serif'}}>Digite o nome da sua empresa</h1><p className="text-sm text-[#6B7280] mt-1.5">Localizaremos seu ambiente corporativo</p></div><div className="bg-white rounded-3xl shadow-[0_20px_60px_-20px_rgba(16,24,40,0.25)] border border-[#E4E7EC] p-5 md:p-6"><div className="relative"><Search className="absolute left-3.5 top-1/2 -translate-y-1/2 w-4 h-4 text-[#9AA1AF]"/><Input autoFocus placeholder="Ex.: Sua Empresa Ltda" value={query} onChange={e=>setQuery(e.target.value)} className="pl-10 h-12 rounded-xl bg-[#F5F6F8] border-transparent focus-visible:ring-2 focus-visible:ring-[color:var(--brand,#2E5AAC)] text-[15px]"/>{searching&&<Loader2 className="absolute right-3.5 top-1/2 -translate-y-1/2 w-4 h-4 text-[#9AA1AF] animate-spin"/>}</div><div className="mt-4 min-h-[80px]"><AnimatePresence mode="wait">{found&&<motion.div key={`ok-${found.id}`} initial={{opacity:0,y:6}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-6}} transition={{duration:.2}} className="flex items-center gap-3 p-3 rounded-2xl border-2" style={{borderColor:found.primary_color,background:`linear-gradient(135deg, ${found.primary_color}33, ${found.secondary_color}22)`}}><div className="w-11 h-11 rounded-xl flex items-center justify-center overflow-hidden shrink-0" style={{background:`linear-gradient(140deg, ${found.primary_color}, ${found.secondary_color})`}}>{found.logo_url?<img src={getCompanyLogoUrl(found.logo_url)??undefined} alt={found.name} className="w-full h-full object-cover"/>:<Building2 className="w-5 h-5 text-white"/>}</div><div className="flex-1 min-w-0"><p className="text-[11px] font-semibold uppercase tracking-wider text-[color:var(--brand,#2E5AAC)] flex items-center gap-1"><Check className="w-3 h-3"/> Empresa encontrada</p><p className="text-[15px] font-semibold text-[#12151C] truncate">{found.name}</p></div></motion.div>}{notFound&&!searching&&<motion.div key="nf" initial={{opacity:0,y:6}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-6}} transition={{duration:.2}} className="flex items-center gap-3 p-3 rounded-2xl border border-[#F0D0D0] bg-[#FFF5F5]"><div className="w-11 h-11 rounded-xl bg-[#FEE2E2] flex items-center justify-center shrink-0"><X className="w-5 h-5 text-[#DC2626]"/></div><div className="flex-1 min-w-0"><p className="text-[11px] font-semibold uppercase tracking-wider text-[#DC2626]">Não encontrada</p><p className="text-[13px] text-[#6B7280]">Verifique o nome digitado</p></div></motion.div>}{!found&&!notFound&&!searching&&<motion.p key="hint" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="text-center text-xs text-[#9AA1AF] pt-6">Comece a digitar para localizar sua empresa</motion.p>}</AnimatePresence></div><Button disabled={!found||continuing} onClick={handleContinue} className="w-full h-12 mt-4 rounded-xl text-[15px] font-semibold transition-all disabled:opacity-40" style={{background:found?`linear-gradient(135deg, ${found.primary_color}, ${found.secondary_color})`:'#12151C',color:'white'}}>{continuing?<Loader2 className="w-4 h-4 animate-spin"/>:<><span>Continuar</span><ArrowRight className="w-4 h-4 ml-1.5"/></>}</Button></div></motion.div><p className="mt-8 text-xs text-[#9AA1AF] relative z-10">© {new Date().getFullYear()} Nuvexa</p></div>;
}
