import { useMemo, useState } from 'react';
import { useSearchParams, useNavigate } from 'react-router-dom';
import { Cake, MessageCircle, Users, LogIn, CalendarDays, Sparkles, X } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Avatar, AvatarFallback, AvatarImage } from '@/components/ui/avatar';
import { Textarea } from '@/components/ui/textarea';
import { useAuth } from '@/contexts/AuthContext';
import { supabase } from '@/integrations/supabase/client';

type BirthdayItem = { id: string; name: string; sector: string; birthDate: string; birthDay: number; celebrationDate?: string | null; avatar?: string | null };
type Snapshot = { companyName: string; monthName: string; theme: string; generatedAt: string; expiresAt: string; people: BirthdayItem[] };
const uuidFromCompact = (value: string) => value.length === 32 ? `${value.slice(0,8)}-${value.slice(8,12)}-${value.slice(12,16)}-${value.slice(16,20)}-${value.slice(20)}` : value;
function decodeSnapshot(value: string | null): Snapshot | null { if (!value) return null; try { const padded = value.replace(/-/g, '+').replace(/_/g, '/') + '='.repeat((4 - value.length % 4) % 4); const binary = atob(padded); const raw = JSON.parse(new TextDecoder().decode(Uint8Array.from(binary, c => c.charCodeAt(0)))); const sectors: string[] = raw.s || []; const people = (raw.p || []).map((p: [string,string,number,string,string?,string?]) => ({ id: uuidFromCompact(p[0]), name: p[1], sector: sectors[p[2]] || 'Sem setor', birthDay: Number(p[3].slice(-2)), birthDate: p[3], celebrationDate: p[4] ? String(p[4]).slice(0, 10) : null, avatar: p[5] ? String(p[5]) : null })); return { companyName: raw.c || 'Empresa', monthName: raw.m || '', theme: raw.t || 'Corporativo', generatedAt: raw.g || '', expiresAt: raw.x || '', people }; } catch { return null; } }

export default function BirthdayShare() {
  const [params] = useSearchParams(); const navigate = useNavigate(); const { user, profile } = useAuth();
  const snapshot = useMemo(() => decodeSnapshot(params.get('data')), [params]); const sector = params.get('sector') || 'Todos';
  const [selectedPerson, setSelectedPerson] = useState<BirthdayItem | null>(null); const [message, setMessage] = useState(''); const [sending, setSending] = useState(false);
  const sectors = useMemo(() => snapshot ? ['Todos', ...Array.from(new Set(snapshot.people.map(p => p.sector))).sort((a,b) => a.localeCompare(b))] : ['Todos'], [snapshot]);
  const filtered = useMemo(() => !snapshot ? [] : sector === 'Todos' ? snapshot.people : snapshot.people.filter(p => p.sector === sector), [snapshot, sector]);
  if (!snapshot) return <Empty title="Relatório indisponível" text="O QR Code não contém um relatório válido." icon={<Cake className="mx-auto mb-4 h-10 w-10 text-primary" />} />;
  if (snapshot.expiresAt && Date.now() > new Date(snapshot.expiresAt).getTime()) return <Empty title="Relatório encerrado" text="Este painel de aniversariantes não está mais disponível para leitura." icon={<CalendarDays className="mx-auto mb-4 h-10 w-10 text-muted-foreground" />} />;
  const isMessageWindowOpen = (person: BirthdayItem) => { const date = person.celebrationDate || person.birthDate; if (!date) return false; const target = new Date(date + 'T00:00:00'); const end = new Date(target); end.setDate(end.getDate() + 1); const now = new Date(); return now >= target && now < end; };
  const openMessage = (person: BirthdayItem) => { const returnTo = `${window.location.pathname}${window.location.search}`; sessionStorage.setItem('birthday_message_target', JSON.stringify({ id: person.id, returnTo })); if (!user) { navigate(`/auth?redirect=${encodeURIComponent(returnTo)}`); return; } setSelectedPerson(person); setMessage(''); };
  const sendMessage = async () => { if (!selectedPerson || !profile || !message.trim()) return; setSending(true); const { error } = await supabase.from('direct_messages').insert({ sender_id: profile.id, receiver_id: selectedPerson.id, content: message.trim(), company_id: profile.company_id }); setSending(false); if (error) return; setSelectedPerson(null); setMessage(''); };
  return <div className="min-h-screen bg-gradient-to-b from-background via-background to-muted/30 pb-10"><header className="sticky top-0 z-20 border-b border-border/60 bg-background/90 backdrop-blur-xl"><div className="mx-auto flex max-w-2xl items-center justify-between gap-3 px-4 py-3"><div className="min-w-0"><p className="truncate text-xs font-semibold uppercase tracking-[0.18em] text-primary">{snapshot.companyName}</p><h1 className="truncate text-lg font-bold">Aniversariantes • {snapshot.monthName}</h1></div>{!user && <Button size="sm" className="shrink-0 rounded-full" onClick={() => navigate(`/auth?redirect=${encodeURIComponent(window.location.pathname + window.location.search)}`)}><LogIn className="mr-1.5 h-4 w-4" />Entrar</Button>}</div></header><main className="mx-auto max-w-2xl space-y-5 px-4 py-5"><Card className="overflow-hidden rounded-3xl border-primary/10 bg-card/80 shadow-sm"><CardContent className="p-5"><div className="flex items-start gap-4"><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-primary/10"><Cake className="h-6 w-6 text-primary" /></div><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-bold">Celebrações do mês</h2><Badge variant="secondary" className="rounded-full">{snapshot.theme}</Badge></div><p className="mt-1 text-sm text-muted-foreground">Filtre por setor e veja quem está celebrando.</p></div></div></CardContent></Card><div className="flex gap-2 overflow-x-auto pb-1">{sectors.map(s => <Button key={s} size="sm" variant={s === sector ? 'default' : 'outline'} className="shrink-0 rounded-full" onClick={() => { const next = new URLSearchParams(params); next.set('sector', s); navigate(`${window.location.pathname}?${next.toString()}`); }}>{s}</Button>)}</div><div className="grid gap-3">{filtered.map(person => <Card key={person.id} className="rounded-2xl border-border/60 shadow-sm"><CardContent className="flex items-center gap-3 p-3.5"><Avatar className="h-12 w-12 shrink-0"><AvatarImage src={person.avatar || undefined} alt={person.name} /><AvatarFallback className="bg-primary/10 font-semibold text-primary">{person.name.split(' ').map(n => n[0]).slice(0, 2).join('').toUpperCase()}</AvatarFallback></Avatar><div className="min-w-0 flex-1"><h3 className="truncate font-semibold">{person.name}</h3><p className="truncate text-sm text-muted-foreground">{person.sector}</p><p className="mt-0.5 text-xs font-medium text-primary">{new Date(person.birthDate + 'T12:00:00').toLocaleDateString('pt-BR', { day: '2-digit', month: 'long' })}</p></div>{isMessageWindowOpen(person) && <Button size="icon" className="h-10 w-10 shrink-0 rounded-full" onClick={() => openMessage(person)} aria-label={`Escrever mensagem para ${person.name}`}><MessageCircle className="h-4 w-4" /></Button>}</CardContent></Card>)}</div>{filtered.length === 0 && <Card className="rounded-2xl"><CardContent className="p-10 text-center text-sm text-muted-foreground"><Users className="mx-auto mb-3 h-8 w-8" />Nenhum aniversariante neste setor.</CardContent></Card>}<div className="flex items-center justify-center gap-2 pt-2 text-center text-xs text-muted-foreground"><Sparkles className="h-3.5 w-3.5" />Relatório digital gerado pelo Nuvexa</div></main>{selectedPerson && <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/40 p-4 sm:items-center"><Card className="w-full max-w-md rounded-3xl shadow-xl"><CardContent className="space-y-4 p-5"><div className="flex items-center justify-between"><div><p className="text-xs text-muted-foreground">Mensagem para</p><h3 className="font-semibold">{selectedPerson.name}</h3></div><Button variant="ghost" size="icon" className="rounded-full" onClick={() => setSelectedPerson(null)}><X className="h-4 w-4" /></Button></div><Textarea autoFocus value={message} onChange={e => setMessage(e.target.value)} placeholder="Escreva sua mensagem de aniversário..." className="min-h-[120px] rounded-2xl" /><Button className="w-full rounded-2xl" disabled={!message.trim() || sending} onClick={sendMessage}>{sending ? 'Enviando...' : 'Enviar mensagem'}</Button></CardContent></Card></div>}</div>;
}
function Empty({ title, text, icon }: { title: string; text: string; icon: React.ReactNode }) { return <div className="min-h-screen bg-background p-6 flex items-center justify-center"><Card className="max-w-md rounded-3xl"><CardContent className="p-8 text-center">{icon}<h1 className="text-xl font-bold">{title}</h1><p className="mt-2 text-sm text-muted-foreground">{text}</p></CardContent></Card></div>; }
